node_modules/
coverage/
*.log
.DS_Store
# local scratch/dev state (real data lives in ~/.claude/herbert/)
.env*
*.local
# note: dist/ is committed on purpose — the plugin's hooks and MCP server run from it
