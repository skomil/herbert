---
description: Show the herbert analytics dashboard URL (starting the server if needed)
---

Call the `dashboard_info` MCP tool to make sure the herbert server is running, then give the user the dashboard URL it returns. If a graphical browser is available on this machine, offer to open it (`open` on macOS, `xdg-open` on Linux).
