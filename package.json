{
  "name": "herbert",
  "version": "0.2.0",
  "description": "Claude Code plugin: local analytics dashboard, spec/correction logging, and session retros",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "dev": "npm run build && node dist/server.js"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
